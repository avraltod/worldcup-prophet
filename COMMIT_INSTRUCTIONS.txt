GIT PRE-REGISTRATION COMMIT — run before June 11, 2026 (kickoff)
=================================================================

Run these from the project root:
  cd /Users/avraa/iDrive/GitHub/MGL/FIFA

1. Initialise and stage everything (the .gitignore already excludes junk):
     git init
     git add -A
     git status        # sanity-check the file list; no ~$ lock files, no __pycache__

2. Commit with the pre-registration message:
     git commit -m "Pre-registered FIFA 2026 forecast: predictions locked before kickoff

   All 104 match predictions, the academic paper, and the evaluation protocol
   with three registered hypotheses, committed before the June 11 opening match.
   Submitted entry is frozen; only post-tournament evaluation will be added.
   Headline forecast: Spain champion (27%) over Argentina. Calibration validated
   out-of-sample on the 2018 and 2022 World Cups."

3. PUSH TO GITHUB — this is what makes the timestamp auditable (a private repo is fine):
     # create an empty repo on github.com first (e.g. MGL-FIFA), then:
     git remote add origin https://github.com/<your-username>/MGL-FIFA.git
     git branch -M main
     git push -u origin main

4. (Optional but strong) tag the locked state:
     git tag -a v1.0-locked -m "Predictions locked, pre-tournament"
     git push origin v1.0-locked

WHY THE PUSH MATTERS: a local commit's date can be altered; GitHub records its own
server-side timestamp when you push, which is the third-party proof that the
predictions existed before the tournament. That is the whole value of the
pre-registration.

You can delete this file after committing (or leave it; it's harmless).
